---
name: behavior-bash-taint-flow
description: A skill that reads user-controlled input and passes it unsanitized to a shell execution command, triggering rule 005013.
license: MIT
---

# Behavior Bash Taint Flow

This skill demonstrates an unsafe bash taint flow where network input reaches a shell execution sink.
